#include <bits/stdc++.h>
using namespace std;

void solve() {
  int n, q;
  cin >> n >> q;
  string s;
  cin >> s;
  pair<int, int> p[q];
  for (int i = 0; i < q; i++) {
    cin >> p[i].first >> p[i].second;
  }
}

int main() {
  int t;
  cin >> t;
  while (t--) {
    solve();
  }
}